import React, { useState } from "react";
import Draggable from "react-draggable";

export default function Securities(props) {
  return (
    <Draggable handle="div">
      <div style={{ border: "1px solid red" }}>fdjghdgkdf</div>
    </Draggable>
  );
}
